import { Linguagem } from "./linguagem";

export class Imagem {
    id: string;
    nome: string;
    size: string;
    formato: string;
    base64: string;
    texto: string;
    linguagem: Linguagem;
}